import 'package:flutter/material.dart';

class VoucherTile extends StatelessWidget {
  const VoucherTile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text(''),
      ],
    );
  }
}